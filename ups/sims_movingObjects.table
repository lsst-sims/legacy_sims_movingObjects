setupRequired(base)
setupRequired(utils)
setupRequired(numpy)
setupRequired(pandas)
setupRequired(oorb)
setupRequired(astropy)

setupRequired(throughputs)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
envPrepend(PATH, ${PRODUCT_DIR}/bin)
